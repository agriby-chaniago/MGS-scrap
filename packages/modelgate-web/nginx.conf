server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — client-side routing (React Router) needs unknown paths
    # to resolve to index.html, not a 404 from the static file server.
    location / {
        try_files $uri /index.html;
    }
}
